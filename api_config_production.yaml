# Production API Configuration for OpenFactVerification
# Keys here are intentionally blank — set them as Render Environment Variables:
#   SERPER_API_KEY  → your Serper.dev API key
#   GROQ_API_KEY    → your Groq API key
#
# render_app.py reads these via os.environ and overrides the values below.

SERPER_API_KEY: ""
GROQ_API_KEY: ""